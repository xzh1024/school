<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no">
    <title>banners</title>
    <style>
        html, body, div, p, ul, li, span, i{ margin: 0; padding: 0;}
        img{ vertical-align: top; width: 100%;}
    </style>
</head>
<body>
    <% for(var i=0; i<banners.length; i++){ %>
    <div><%= banners[i].title %></div>
    <div><%= banners[i].sub_title %></div>
    <img src="<%= banners[i].src %>">
    <% } %>
</body>
</html>